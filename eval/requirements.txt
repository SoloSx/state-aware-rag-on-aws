# RAGAS 定量評価用の依存（論文指標 Sub-EM/Acc だけなら不要）
# ragas 0.2 系は langchain 0.3 系と組み合わせる（langchain 1.x とは非互換）。
ragas==0.2.15
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-aws>=0.2,<0.3
datasets>=2.16
